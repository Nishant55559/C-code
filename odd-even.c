#include <stdio.h>

int main(){
    int a;
    printf("Enter the value of a: ");
    scanf("%d", &a);
    if (a%2==0)
        printf("it is even");
    else 
        printf("the number id odd");

    


    return 0;
}